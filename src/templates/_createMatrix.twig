{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "matrix handle."|t('migrate-from-wordpress'),
    id: 'matrixHandle',
    name: 'matrixHandle',
}) }}

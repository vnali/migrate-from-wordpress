{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "block type handle."|t('migrate-from-wordpress'),
    id: 'blockTypeHandle',
    name: 'blockTypeHandle',
}) }}

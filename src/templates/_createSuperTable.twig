{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "super table handle."|t('migrate-from-wordpress'),
    id: 'superTableHandle',
    name: 'superTableHandle',
}) }}

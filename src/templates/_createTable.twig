{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "table handle."|t('migrate-from-wordpress'),
    id: 'tableHandle',
    name: 'tableHandle',
}) }}

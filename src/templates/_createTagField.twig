{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: 'Tag'|t('migrate-from-wordpress'),
    id: 'tagGroup',
    name: 'tagGroup',
    options: tags,  
}) }}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "field handle:"|t('migrate-from-wordpress'),
    id: 'fieldHandle',
    name: 'fieldHandle',
}) }}

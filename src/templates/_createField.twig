{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "handle"|t('migrate-from-wordpress'),
    instructions: "field handle:"|t('migrate-from-wordpress'),
    id: 'fieldHandle',
    name: 'fieldHandle',
}) }}
